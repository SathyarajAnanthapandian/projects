# dev environment. Run with: --env=dev
# Replace the placeholder values below with your dev resources.

project=my-dev-project
inputTable=my-dev-project:my_dataset.my_table
outputTopic=projects/my-dev-project/topics/my-topic

# Use a query instead of inputTable if you only need some rows (set one or the other, not both):
# query=SELECT headers, requestPayload FROM `my-dev-project.my_dataset.my_table` WHERE ...

# Only needed with runner=DataflowRunner:
region=us-central1
tempLocation=gs://my-dev-bucket/temp
