# Settings shared by every environment.
# Each key is a pipeline option: key=value is passed as --key=value.
# application-<env>.properties overrides these, and command-line arguments override both.

# DirectRunner runs the pipeline inside your JVM (IntelliJ). Use DataflowRunner to run on GCP.
runner=DirectRunner

headersColumn=headers
payloadColumn=requestPayload
